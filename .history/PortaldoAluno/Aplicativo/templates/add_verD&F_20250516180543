<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Desempenho e Frequência</title>
    <style>
        table, th, td {
            border: 1px solid #000;
            border-collapse: collapse;
            padding: 8px;
        }
        th {
            background-color: #f2f2f2;
        }
        textarea {
            width: 300px;
            height: 60px;
        }
    </style>
</head>
<body>
    <h1>Desempenho e Frequência dos Alunos</h1>

    <h2>Adicionar Novo Registro</h2>
    <form method="post">
        {% csrf_token %}
        <label for="aluno">Aluno:</label>
        <select name="aluno" id="aluno" required>
            {% for aluno in alunos %}
                <option value="{{ aluno.id }}">{{ aluno.nome }}</option>
            {% endfor %}
        </select><br><br>

        <label for="faltas">Faltas:</label>
        <input type="number" name="faltas" id="faltas" required><br><br>

        <label for="desempenho">Desempenho:</label>
        <input type="text" name="desempenho" id="desempenho" required><br><br>

        <label for="emoji">Emoji:</label>
        <input type="text" name="emoji" id="emoji" maxlength="5"><br><br>

        <label for="comentario">Comentário do Professor:</label><br>
        <textarea name="comentario" id="comentario"></textarea><br><br>

        <button type="submit">Salvar</button>
    </form>

    <hr>

    <h2>Registros de Desempenho</h2>

    {% if desempenhos %}
        <table>
            <thead>
                <tr>
                    <th>Aluno</th>
                    <th>Faltas</th>
                    <th>Desempenho</th>
                    <th>Emoji</th>
                    <th>Comentário</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                {% for item in desempenhos %}
                    <tr>
                        <td>{{ item.aluno.nome }}</td>
                        <td>{{ item.faltas }}</td>
                        <td>{{ item.desempenho }}</td>
                        <td>{{ item.emoji }}</td>
                        <td>{{ item.comentario_professor }}</td>
                        <td>
                            <a href="{% url 'desempenho_edit' item.id %}">Editar</a> |
                            <a href="{% url 'desempenho_delete' item.id %}" onclick="return confirm('Tem certeza que deseja excluir este registro?');">Excluir</a>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p>Nenhum registro encontrado.</p>
    {% endif %}
</body>
</html>
